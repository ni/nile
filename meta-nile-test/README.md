# meta-nile-test

This layer enables "extended" board support for testing purposes.

Extended boards are supported only experimentally for specific test
scenarios. Downstream clients should not consume this layer.
